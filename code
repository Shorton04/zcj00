You are an SAP ABAP developer.
Create the full updated ABAP code for this routine:
FORM name: get_raw_data
Signature: FORM get_raw_data USING iv_pspid_proj TYPE ztb_a2r_wbshdr-pspid.
Global outputs: gs_header (type like ZTB_A2R_WBSHDR) and gt_wbs_items (type table of ZTB_A2R_WBSITEM).
Requirement: Provide the complete ABAP code of the FORM, using modern Open SQL (SELECT SINGLE, INTO TABLE @itab) and avoid invalid patterns (ENDSELECT with INTO TABLE).

1) PROJ Header Read
Select single from PROJ where pspid = iv_pspid_proj:

fields: pspnr, post1, ernam, erdat, vernr, vbukr, vkokr, prctr, plsez, werks, profl, kostl

Map to gs_header:

gs_header-mandt = sy-mandt
gs_header-pspnr = proj-pspnr
gs_header-post1 = proj-post1
gs_header-ernam = proj-ernam
gs_header-erdat = proj-erdat
gs_header-vernr = proj-vernr
gs_header-bukrs = proj-vbukr
gs_header-vkokr = proj-vkokr
gs_header-prctr = proj-prctr
gs_header-plsez = proj-plsez
gs_header-werks = proj-werks
gs_header-profl = proj-profl
gs_header-kostl = proj-kostl

If PROJ not found, RETURN.

2) PRPS Item Read
Select all PRPS rows where psphi = proj-pspnr (internal project number):

fields: posid, post1, objnr, pbukr, pkokr, pkstl, werks, stufe, zunifier_proj
Store into internal table lt_prps. If empty, RETURN.


3) Populate gt_wbs_items from PRPS
Loop lt_prps and append into gt_wbs_items with mapping:

gt_wbs_items-mandt = sy-mandt
gt_wbs_items-pspid = iv_pspid_proj
gt_wbs_items-posid = prps-posid
gt_wbs_items-post1 = prps-post1
gt_wbs_items-bukrs = prps-pbukr
gt_wbs_items-vkokr = prps-pkokr
gt_wbs_items-stufe = prps-stufe
gt_wbs_items-werks = prps-werks

Also: set gs_header-zunifier_proj from the first non-initial prps-zunifier_proj encountered (do not overwrite once filled).

4) BPGE KSTP → WERT2 (Highest Ledger)
For each prps-objnr:

Filter BPGE by objnr = prps-objnr and vorga = 'KSTP'
Determine highest ledger value (use MAX( ledger_field ) grouped by OBJNR; ledger field is LEDNR unless I say otherwise)
Then read BPGE again for that (objnr, highest lednr, vorga = 'KSTP') and compute SUM( wtges )
Assign: gt_wbs_items-wert2 = summed_wtges

Implement this efficiently:

Create unique OBJNR list from lt_prps
Select max-ledger per OBJNR into table
Select aggregated wtges into table
Use HASHED tables for lookups during item loop


5) BPGE KBUD → GEBER (Highest Ledger, GEBER not initial)
Separately, for each prps-objnr:

Filter BPGE by objnr = prps-objnr, vorga = 'KBUD', and geber <> ''
Determine highest ledger per OBJNR
Select MAX( geber ) for that (objnr, highest ledger, vorga='KBUD')
Store results in a hashed lookup table keyed by OBJNR (do NOT assign anywhere unless I have a target field; just prepare it for later).


6) V_IVP_PR Selection (No Max GJAHR Rule)
For each prps-posid, select from V_IVP_PR:

fields: prnam, gjahr, posnr, prart
where posid = prps-posid

IMPORTANT: Do NOT compute MAX(GJAHR).
If multiple rows exist for the same POSID, keep the first record only (any deterministic method is ok, e.g., sort by POSID then keep first).
Map results:

Item-level:

gt_wbs_items-gjahr = v_ivp_pr-gjahr
gt_wbs_items-prart = v_ivp_pr-prart


Header-level (fill only once; do not overwrite once set):

gs_header-prnam = v_ivp_pr-prnam
gs_header-gjahr = v_ivp_pr-gjahr
gs_header-posnr = v_ivp_pr-posnr



Implement as bulk read:

Use FOR ALL ENTRIES on lt_prps by POSID OR build POSID list and use IN.
Use hashed table keyed by POSID for lookup during item loop.


7) IMPR Selection for IZWEK
From IMPR, select:

izwek
where posnr = v_ivp_pr-posnr

Bulk approach:

Build POSNR list from selected V_IVP_PR results
Select IMPR for all POSNRs
Use hashed table keyed by POSNR

Map to header (fill only once):

gs_header-izwek = impr-izwek


8) Output Requirements

Return complete ABAP FORM code (including all local types/decl, selects, loops).
Ensure syntax is correct (no ENDSELECT with INTO TABLE).
Use guards for FOR ALL ENTRIES (itab IS NOT INITIAL).
Use hashed tables for fast lookups.
Add short inline comments explaining each block and mapping.

If you need the ledger field name, assume BPGE-LEDNR.

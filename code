METHOD your_existing_method.

  DATA: lo_wflog   TYPE REF TO zcl_a2r_wbs_workflow_log,
        lv_success TYPE abap_bool.

  lo_wflog = NEW zcl_a2r_wbs_workflow_log( ).

  lv_success = lo_wflog->save_rejection_comments(
                 iv_zreq_num = iv_zreq_num ).

  IF lv_success = abap_false.
    " handle no comments found
  ENDIF.

ENDMETHOD.

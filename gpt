═══════════════════════════════════════════════════════════════════
REFERENCE CODE ANALYSIS: wbs_up and wbs_left EXIST!
═══════════════════════════════════════════════════════════════════

You are CORRECT! The reference code clearly shows:

```abap
ls_wbs_celement-wbs_up   = lv_upper_level.
ls_wbs_celement-wbs_left = lv_left_level.
```

These fields ARE supported in the reference system’s BAPI_WBS_ELEMENT structure.

═══════════════════════════════════════════════════════════════════
WHY THE CONFUSION?
═══════════════════════════════════════════════════════════════════

1. **Standard SAP vs Enhanced**: Your system may have an enhanced version
   of BAPI_WBS_ELEMENT that includes wbs_up and wbs_left
1. **Earlier I said they don’t exist**: I was looking at standard S/4HANA
   structure browser screenshots, which showed these fields missing
1. **Your system IS enhanced**: The reference code proves your system
   has these fields

═══════════════════════════════════════════════════════════════════
KEY DIFFERENCES BETWEEN YOUR CODE AND REFERENCE
═══════════════════════════════════════════════════════════════════

ISSUE #1: WRONG FIELD NAMES
─────────────────────────────────────────────────────────────────
YOUR CODE:
ls_wbs_celement-comp_code     ❌
ls_wbs_celement-bus_area      ❌
ls_wbs_celement-request_cctr_controlling_area ❌

REFERENCE:
ls_wbs_celement-company_code     ✓
ls_wbs_celement-business_area    ✓
ls_wbs_celement-controlling_area ✓

ISSUE #2: WRONG SORT ORDER
─────────────────────────────────────────────────────────────────
YOUR CODE:
SORT lt_wbs_sorted BY stufe ASCENDING.

REFERENCE:
SORT lt_item BY pspid_new DESCENDING stufe DESCENDING.

This affects hierarchy calculation!

ISSUE #3: HIERARCHY CALCULATION LOGIC
─────────────────────────────────────────────────────────────────
REFERENCE CODE LOGIC:

LOOP AT lt_item INTO ls_item_temp FROM lv_index.

“ Find LEFT sibling (same level)
IF lv_left_level IS INITIAL AND
ls_item_temp-stufe = ls_item-stufe.
lv_left_level = ls_item_temp-pspid_new.  ← Direct assignment!
ENDIF.

“ Find UPPER parent (one level up)
IF ls_item_temp-stufe < ls_item-stufe.
lv_upper_level = ls_item_temp-pspid_new.  ← Direct assignment!
EXIT.
ENDIF.

ENDLOOP.

Key insight: Reference assigns the PSPID directly from the source data,
not calculating new WBS codes like your code does!

═══════════════════════════════════════════════════════════════════
REFERENCE CODE FLOW
═══════════════════════════════════════════════════════════════════

INPUT DATA (lt_wbs_item):
[1] PSPID_NEW = ‘PROJ-001’, STUFE = 1  (Project)
[2] PSPID_NEW = ‘PROJ-001-1’, STUFE = 2  (WBS 1)
[3] PSPID_NEW = ‘PROJ-001-2’, STUFE = 2  (WBS 2)
[4] PSPID_NEW = ‘PROJ-001-2-1’, STUFE = 3  (Sub-WBS)

SORTED (DESCENDING):
[1] PSPID_NEW = ‘PROJ-001-2-1’, STUFE = 3
[2] PSPID_NEW = ‘PROJ-001-2’, STUFE = 2
[3] PSPID_NEW = ‘PROJ-001-1’, STUFE = 2
[4] PSPID_NEW = ‘PROJ-001’, STUFE = 1

PROCESSING [1] (STUFE=3, PSPID=‘PROJ-001-2-1’):
lv_index = 2

Loop from index 2:
[2] STUFE=2 < STUFE=3 → lv_upper_level = ‘PROJ-001-2’ ← PARENT!
EXIT

Result:
wbs_element = ‘PROJ-001-2-1’
wbs_up      = ‘PROJ-001-2’
wbs_left    = (blank)

PROCESSING [2] (STUFE=2, PSPID=‘PROJ-001-2’):
lv_index = 3

Loop from index 3:
[3] STUFE=2 = STUFE=2 → lv_left_level = ‘PROJ-001-1’ ← SIBLING!
[4] STUFE=1 < STUFE=2 → lv_upper_level = ‘PROJ-001’ ← PARENT!
EXIT

Result:
wbs_element = ‘PROJ-001-2’
wbs_up      = ‘PROJ-001’
wbs_left    = ‘PROJ-001-1’

═══════════════════════════════════════════════════════════════════
THE CRITICAL ISSUE IN YOUR CODE
═══════════════════════════════════════════════════════════════════

YOUR DATA FLOW:
lt_wbs_sorted has:
[1] POSID = ‘PC000001’, STUFE = 1
[2] POSID = ‘PC000002’, STUFE = 2
[3] POSID = ‘PC000003’, STUFE = 2

YOUR CODE TRIES:
ls_wbs_temp-stufe < ls_wbs-stufe
lv_parent_posid = |{ lv_pspid }-{ lv_parent_counter }|  ← CALCULATES!

BUT:

- You’re calculating WBS codes instead of using source POSID
- Your source data (it_wbs_items) has POSID from template
- You should be USING those POSIDs for hierarchy, not calculating new ones

REFERENCE USES:
lv_upper_level = ls_item_temp-pspid_new  ← DIRECT from source!

═══════════════════════════════════════════════════════════════════
YOUR ACTUAL PROBLEM
═══════════════════════════════════════════════════════════════════

However, in YOUR application:

- Source data (it_wbs_items) has template POSIDs (PC000001, PC000002)
- You need to generate NEW POSIDs for the actual project
- So you CAN’T use source POSIDs directly

Solution:

- Build the mapping FIRST
- Generate all new POSIDs before hierarchy calculation
- Then assign hierarchy based on NEW POSIDs

═══════════════════════════════════════════════════════════════════
CORRECTED APPROACH
═══════════════════════════════════════════════════════════════════

PHASE 1: Build POSID mapping
─────────────────────────────────────────────────────────────────
LOOP AT it_wbs_items INTO ls_wbs.
IF ls_wbs-stufe = 1.
ls_wbs-pspid_new = lv_pspid.  “ P202600001
ELSE.
lv_counter = lv_counter + 1.
ls_wbs-pspid_new = |{ lv_pspid }-{ lv_counter }|.  “ P202600001-1
ENDIF.
MODIFY it_wbs_items FROM ls_wbs.
ENDLOOP.

PHASE 2: Now use reference logic with NEW POSIDs
─────────────────────────────────────────────────────────────────
SORT it_wbs_items BY pspid_new DESCENDING stufe DESCENDING.

LOOP AT it_wbs_items INTO ls_wbs.
lv_index = sy-tabix + 1.

LOOP AT it_wbs_items INTO ls_wbs_temp FROM lv_index.
“ Left level
IF lv_left_level IS INITIAL AND ls_wbs_temp-stufe = ls_wbs-stufe.
lv_left_level = ls_wbs_temp-pspid_new.  ← Use NEW POSID!
ENDIF.

```
" Upper level
IF ls_wbs_temp-stufe < ls_wbs-stufe.
  lv_upper_level = ls_wbs_temp-pspid_new.  ← Use NEW POSID!
  EXIT.
ENDIF.
```

ENDLOOP.

ls_wbs_celement-wbs_element = ls_wbs-pspid_new.
ls_wbs_celement-wbs_up      = lv_upper_level.
ls_wbs_celement-wbs_left    = lv_left_level.

APPEND ls_wbs_celement TO lt_wbs_celement.
ENDLOOP.

═══════════════════════════════════════════════════════════════════
SUMMARY - WHAT TO FIX
═══════════════════════════════════════════════════════════════════

1. Fix field names:
   comp_code → company_code
   bus_area → business_area
   request_cctr_controlling_area → controlling_area
1. Pre-generate all WBS codes before hierarchy calculation
1. Use reference logic exactly, but with your pre-generated codes
1. Sort DESCENDING like reference
1. Keep wbs_up and wbs_left (they exist in your system!)

═══════════════════════════════════════════════════════════════════
END OF ANALYSIS
═══════════════════════════════════════════════════════════════════
FORM total_up_wbs_tc.

  DATA: lv_parent_len TYPE i,
        lv_sum        TYPE ty_wbs_display-wert2,
        lv_pspid_p    TYPE prps-posid,
        lv_pspid_c    TYPE prps-posid.

  FIELD-SYMBOLS:
    <fs_parent> TYPE ty_wbs_display,
    <fs_child>  TYPE ty_wbs_display.

  DO 4 TIMES.
    DATA(lv_level) = 5 - sy-index.

    LOOP AT gt_wbs_display ASSIGNING <fs_parent>
      WHERE stufe = lv_level.

      CLEAR lv_sum.

      lv_pspid_p = <fs_parent>-pspid.

      CALL FUNCTION 'FTR_CORR_SWIFT_DELETE_ENDZERO'
        CHANGING
          c_value = lv_pspid_p.

      lv_parent_len = strlen( lv_pspid_p ).

      LOOP AT gt_wbs_display ASSIGNING <fs_child>
        WHERE stufe = lv_level + 1.

        lv_pspid_c = <fs_child>-pspid.

        CALL FUNCTION 'FTR_CORR_SWIFT_DELETE_ENDZERO'
          CHANGING
            c_value = lv_pspid_c.

        IF lv_pspid_c+0(lv_parent_len) = lv_pspid_p.
          lv_sum = lv_sum + <fs_child>-wert2.
        ENDIF.

      ENDLOOP.

      IF lv_sum IS NOT INITIAL.
        <fs_parent>-wert2 = lv_sum.
      ENDIF.

    ENDLOOP.
  ENDDO.

ENDFORM.




